name: QA Deploy

on:
  push:
    branches:
      - qa

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy triggered
        run: echo "Push a qa detectado - Dokploy hace el deploy automaticamente"